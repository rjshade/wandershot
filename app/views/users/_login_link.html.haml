- classes = ((defined? classes) ? classes : "")
- content = ((defined? content) ? content : "")

=link_to(content, new_user_session_path, :class => "login-button #{classes}", :"data-target" => "#userModal", :"data-toggle" => "modal")
