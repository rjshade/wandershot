.page-header
  %h1 Comments

- @comments.each do |comment|
  = render partial: 'comments/show', locals: { comment: comment }
  %p
    = comment.text
    = link_to '&times;'.html_safe, comment, :confirm => "Are you sure?", :method => :delete, :remote => true
